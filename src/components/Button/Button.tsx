import React, {ComponentProps} from 'react';
import './Button.scss';

function Button(props: ComponentProps<any>) {
  return (
    <>
      <button>
        Test button
      </button>
    </>
  );
}

export default Button;
